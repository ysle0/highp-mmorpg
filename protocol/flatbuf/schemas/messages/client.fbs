include "../user.fbs";
include "../room.fbs";

namespace highp.protocol.messages;

// 방 입장
table JoinRoomRequest {
    username: string (required);
}

// 방 퇴장
table LeaveRoomRequest {
    room_id: uint32;
}

// 메시지 전송
table SendMessageRequest {
    room_id: uint32;
    message: string (required);
}
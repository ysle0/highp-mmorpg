include "../common.fbs";
include "../user.fbs";
include "../room.fbs";
include "../error.fbs";

namespace highp.protocol.messages;

// 방 입장 결과
table JoinedRoomResponse {
    ok: bool;
    room: RoomWithUsers;
    error: Error;
}

// 방 퇴장 결과
table LeftRoomResponse {
    ok: bool;
    error: Error;
}
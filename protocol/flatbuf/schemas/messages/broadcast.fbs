include "../common.fbs";
include "../user.fbs";

namespace highp.protocol.messages;

// 유저 입장 알림
table UserJoinedBroadcast {
    user: User (required);
}

// 유저 퇴장 알림
table UserLeftBroadcast {
    user_id: uint32;
    username: string (required);
}

// 채팅 메시지  
table ChatMessageBroadcast {
    sender_id: uint32;
    message: string (required);
    timestamp: Common.Timestamp;
}
